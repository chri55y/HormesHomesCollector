<%= form_with(model: property, local: true) do |form| %>
  <% if property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property.errors.count, "error") %> prohibited this property from being saved:</h2>

      <ul>
      <% property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :property_address %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score, id: :property_score %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :property_price %>
  </div>

  <div class="field">
    <%= form.label :sqft %>
    <%= form.number_field :sqft, id: :property_sqft %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :property_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
