<%= form_with(model: property_note, local: true) do |form| %>
  <% if property_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_note.errors.count, "error") %> prohibited this property_note from being saved:</h2>

      <ul>
      <% property_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.number_field :property_id, id: :property_note_property_id %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note, id: :property_note_note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
